import json

import tensorflow as tf

class DatasetReader:
    """
    Should contain the input stage for reading, preprocessing and possibly
    augmenting (maybe as a closure) for an arbitrary.
    Not sure if input transformations should be applied that involves
    rescaling.
    """

    def __init__(self, path):

    def __iter__(self):

